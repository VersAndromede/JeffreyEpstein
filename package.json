{
  "scripts": {
    "build": "npm install --prefix client && npm install --prefix server && npm run --prefix client build && npm run --prefix server build",
    "start": "npm run --prefix server start:prod"
  }
}
